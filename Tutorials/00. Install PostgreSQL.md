# Install PostgreSQL
```bash
sudo apt install postgresql-common
sudo /usr/share/postgresql-common/pgdg/apt.postgresql.org.sh
sudo apt update
#sudo apt upgrade #if updates available
sudo apt -y install postgresql postgresql-doc libpq-dev postgresql-server-dev-18
```
